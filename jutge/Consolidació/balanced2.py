from yogi import scan


def is_balanced(n: int) -> bool:

def main() -> None:
    n = scan(int)
    
    while n is not None:
        print(is_balanced(n))
        n = scan(int)

if __name__ == "__main__":
    main()
